import { NavigationBackDirective } from './navigation-back.directive';
describe('NavigationBackDirective', () => {
  it('should create an instance', () => {
    const directive = new NavigationBackDirective();
    expect(directive).toBeTruthy();
  });
});
