import { SplitNameFromUrlPipe } from './split-name-from-url.pipe';
describe('SplitNameFromUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitNameFromUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
