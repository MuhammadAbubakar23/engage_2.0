import { GetFileTypeFromUrlPipe } from './get-file-type-from-url.pipe';

describe('GetFileTypeFromUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFileTypeFromUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
