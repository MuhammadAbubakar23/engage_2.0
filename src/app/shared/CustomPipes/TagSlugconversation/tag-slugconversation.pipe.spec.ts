import { TagSlugconversationPipe } from './tag-slugconversation.pipe';

describe('TagSlugconversationPipe', () => {
  it('create an instance', () => {
    const pipe = new TagSlugconversationPipe();
    expect(pipe).toBeTruthy();
  });
});
