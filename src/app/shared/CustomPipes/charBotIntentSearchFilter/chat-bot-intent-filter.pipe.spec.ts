import { ChatBotIntentFilterPipe } from './chat-bot-intent-filter.pipe';

describe('ChatBotIntentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ChatBotIntentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
