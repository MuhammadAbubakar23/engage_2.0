import { ResponsefilterPipe } from './responsefilter.pipe';

describe('ResponsefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ResponsefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
