import { QuickReplySearchFilterPipe } from './quick-reply-search-filter.pipe';

describe('QuickReplySearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new QuickReplySearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
